# Copy to .env (already gitignored — never commit real values).
# Optional. Without these, business-context competitor lookup and pricing-source
# freshness checks fall back to static reference data; the report says so explicitly.
BRIGHTDATA_API_KEY=
BRIGHTDATA_SERP_ZONE=
